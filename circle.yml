machine:
    java:
        version: openjdk7
    environment:
        ANDROID_HOME: /Users/shimadawaya/Library/Android/sdk
        JAVA_OPTS: "-Xmx2048m -XX:MaxPermSize=1024m"

dependencies:
  override:
    - echo y | android update sdk --no-ui --filter android-21,build-tools-23.0.0

test:
    override:
      - echo "Nothing to do here"

deployment:
    master:
        branch: master
        commands:
          - ./gradlew assembleRelease
